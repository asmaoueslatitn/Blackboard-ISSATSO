@model BlackBoard.Core.ViewModel.AttendanceFormViewModel
@{
    ViewBag.Title = Model.Heading;
}

<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h3>@Model.Heading </h3>
                <div class="clearfix"></div>
            </div>

            <div class="x_content">
                <div class="row" id="studentDetail">
                    @*<h3> @ViewBag.Confirm </h3>*@
                    @*@using (Ajax.BeginForm("Create", "Attendances", new AjaxOptions{InsertionMode = InsertionMode.Replace, UpdateTargetId = "studentDetail"}))*@
                    @using (Html.BeginForm("Create", "Attendances", FormMethod.Post, new { @class = "form-horizontal form-label-left", autocomplete = "off" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        @Html.HiddenFor(model => model.Student)

                        <div class="form-group">
                            <label class="control-label col-sm-3 col-xs-12">Exam Remarks</label>
                            <div class="col-md-6 col-sm-6 col-xs-12">
                                @Html.TextAreaFor(m => m.ExamRemarks, new { @class = "form-control col-md-7 col-xs-12", @rows=8 })
                                @Html.ValidationMessageFor(m => m.ExamRemarks)
                            </div>
                        </div>

                        <div class="form-group">
                            <label class="control-label col-sm-3 col-xs-12">Interpretation</label>
                            <div class="col-md-6 col-sm-6 col-xs-12">
                                @Html.TextAreaFor(m => m.Interpretation, new { @class = "form-control col-md-7 col-xs-12" })
                                @Html.ValidationMessageFor(m => m.Interpretation)
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="control-label col-sm-3 col-xs-12">Second Interpretation</label>
                            <div class="col-md-6 col-sm-6 col-xs-12">
                                @Html.TextAreaFor(m => m.SecondInterpretation, new { @class = "form-control col-md-7 col-xs-12" })
                                @Html.ValidationMessageFor(m => m.SecondInterpretation)
                            </div>
                        </div>

                        <div class="form-group">
                            <label class="control-label col-sm-3 col-xs-12">Third Interpretation</label>
                            <div class="col-md-6 col-sm-6 col-xs-12">
                                @Html.TextAreaFor(m => m.ThirdInterpretation, new { @class = "form-control col-md-7 col-xs-12" })
                                @Html.ValidationMessageFor(m => m.ThirdInterpretation)
                            </div>
                        </div>

                        <div class="form-group">
                            <label class="control-label col-sm-3 col-xs-12">RecoverCandidate</label>
                            <div class="col-md-6 col-sm-6 col-xs-12">
                                @Html.TextAreaFor(m => m.RecoverCandidate, new { @class = "form-control col-md-7 col-xs-12" })
                                @Html.ValidationMessageFor(m => m.RecoverCandidate)
                            </div>
                        </div>
                        <div class="ln_solid"></div>
                        <div class="form-group">
                            <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-5">
                                <button type="submit" class="btn btn-success">Save</button>
                            </div>
                        </div>
                    }

                </div>
                </div>
        </div>
    </div>
</div>
@section scripts{
    @Scripts.Render("~/bundles/jqueryval")

}
